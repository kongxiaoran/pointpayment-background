<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.impl.MenuDaoImpl">

    <sql id="menu">menu</sql>

    <insert id="insert" parameterType="Menu" keyProperty="id"  useGeneratedKeys="true">
        INSERT INTO <include refid="menu"/> (dishes,price,picture,typeid,hid,status) values(#{dishes},#{price},#{picture},#{typeid},#{hid},#{status})
    </insert>

    <update id="update" parameterType="Menu">
        update <include refid="menu" />  set status=1
        <if test="dishes != null">,dishes=#{dishes}</if>
        <if test="price != null">,price=#{price}</if>
        <if test="picture != null">,picture=#{picture}</if>
        <if test="typeid != null">,typeid=#{typeid}</if>
        <if test="status != null">,status=#{status}</if>
        where status!=9 and id=#{id}
    </update>

    <select id="getBy" parameterType="Menu" resultType="Menu">
        SELECT id,dishes,price,picture,typeid,hid,status,DATE_FORMAT(createtime,"%Y-%m-%d %H:%m") as createtime FROM <include refid="menu"/> s WHERE s.status!=9
        <if test="dishes!=null || dishes!=''">and s.dishes=#{dishes}</if>
        <if test="price!=null || price!=''">and s.price=#{price}</if>
        <if test="picture!=null || picture!=''">and s.picture=#{picture}</if>
        <if test="typeid!=null || typeid!=''">and s.typeid=#{typeid}</if>
        <if test="hid!=null || hid!=''">and s.hid=#{hid}</if>
        <if test="status!=null || status!=''">and s.status=#{status}</if>
    </select>

    <select id="getById" parameterType="long" resultType="Menu">
        SELECT id,dishes,price,picture,typeid,hid,status,DATE_FORMAT(createtime,"%Y-%m-%d %H:%m") as createtime FROM <include refid="menu"/> m WHERE m.status!=9
        AND m.id = #{id}
    </select>


    <select id="getListByType" parameterType="Menu" resultType="Menu">
        SELECT id,dishes,price,picture,typeid,hid,status,DATE_FORMAT(createtime,"%Y-%m-%d %H:%m") as createtime FROM <include refid="menu"/> s WHERE s.status!=9
        <if test="typeid!=null || typeid!=''">and s.typeid=#{typeid}</if>
    </select>

</mapper>